{
  "name": "ui5tutorial",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "ui5 serve -o index.html",
    "start-test": "ui5 serve -o test/mockServer.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ui5 build -a",
    "serve-dist": "ws --compress -d dist --open"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@ui5/cli": "^3.8.0",
    "local-web-server": "^5.3.1",
    "ui5-middleware-simpleproxy": "^3.2.11"
  }
}
